{% extends 'base.html.twig' %}
{% block body %}
<h6>{{ discussion.title }}</h6>
<span>Ecrit le {{  discussion.createdAt | date}}</span>
<div class="container p-3 my-3 bg-dark text-white">{{ discussion.content | raw}}</div>
{% endblock %}