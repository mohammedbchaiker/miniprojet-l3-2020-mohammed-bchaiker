{% extends 'base.html.twig'%}
{% block body %}
    {% for user in users %}
        <div class="card" >
            <div class="card-body">
                <h5 class="card-title">{{ user.email }}</h5>
                <p>{{ user.username }}</p>
            </div>
        </div>
    {% endfor %}
{% endblock %}