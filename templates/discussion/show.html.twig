{% extends 'base.html.twig' %}

{% block title %}Discussion{% endblock %}

{% block body %}
    <h1>Discussion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ discussion.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ discussion.createdAt ? discussion.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ discussion.content }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ discussion.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('discussion_index') }}"  >Lister les discussions</a>

    <a href="{{ path('discussion_edit', {'id': discussion.id}) }}">Modifier</a>

    {{ include('discussion/_delete_form.html.twig') }}
{% endblock %}
