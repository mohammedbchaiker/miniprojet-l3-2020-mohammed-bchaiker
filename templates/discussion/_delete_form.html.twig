
<form method="post" action="{{ path('discussion_delete', {'id': discussion.id}) }}" onsubmit="return confirm('Cette discussion sera supprimée vous voulez continuez ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ discussion.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
